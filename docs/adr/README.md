# Architecture Decision Records

This directory records decisions that affect the architecture or long-term
maintenance of `mcp-monday-projects`.

Use the next sequential number, keep one decision per file, and update the
status when a decision is superseded.
